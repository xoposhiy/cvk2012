source("make-graph.r")
# prepare csv files for import in gephi. Coordinates — result of PCA
fnodes = "../data/gnodes.csv"
fedges = "../data/gedges.csv"
fply = "../data/g.ply"
v <- read.csv("../data/vectors.csv", encoding="Windows-1251")
Label = v[,1]
Curia = v[,2]
Block = v[,3]
ans = v[,4:28]
blockNames = unique(Block)
Id = 1:length(Label)
pca <- princomp(ans)
x <- pca$scores[,1]
y <- pca$scores[,2]
z <- pca$scores[,3]
m <- pca$scores[,4]
setwd("c:/work/cvk2012/data")
# prepare csv files for import in gephi. Coordinates — result of PCA
fnodes = "../data/gnodes.csv"
fedges = "../data/gedges.csv"
fply = "../data/g.ply"
v <- read.csv("../data/vectors.csv", encoding="Windows-1251")
Label = v[,1]
Curia = v[,2]
Block = v[,3]
ans = v[,4:28]
blockNames = unique(Block)
Id = 1:length(Label)
pca <- princomp(ans)
x <- pca$scores[,1]
y <- pca$scores[,2]
z <- pca$scores[,3]
m <- pca$scores[,4]
x
setwd("../src")
